const INITIAL_STATE = [{ id: 1, text: 'hello' }, { id: 2, text: 'world' }, { fjoiw: 3 }]
export default (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
}
